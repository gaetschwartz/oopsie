error: `packed` requires backtrace and spantrace to share one boxing mode; set both `boxed` the same, or use `packed = false` for per-trace boxing
 --> tests/compile-fail/stable/packed_incoherent_boxing.rs:8:10
  |
8 | #[oopsie(traced(backtrace, spantrace(boxed = false)))]
  |          ^^^^^^
